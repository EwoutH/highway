image: Visual Studio 2019
configuration: Release

environment:
  matrix:
  - arch: x64
  - arch: ARM64

before_build: cmake . -A %arch% -DHWY_SYSTEM_GTEST=OFF

build:
  project: hwy.sln
  verbosity: minimal

artifacts:
- path: /*/Release/*.exe
